# G1 — M1 spawn on the 100k fixture with a spare

Date: 2026-09-06
Goal: spec §7 G1. Requirement: spec §3 R12.
Issue: [#38](https://github.com/navaro1/gh-klon/issues/38).

Condition: `gh klon bench --cell m1-add-100k --json` reports `p50_ms <= 1000`
with `timing_valid: true` and `spare: true` on ext4, and `p50_ms <= 200` on a
btrfs loop volume.

Host: Ubuntu 22.04, ext4 on NVMe, git 2.34.1, 20 CPUs, 62 GB RAM. Other
agents ran builds on the host during the whole session; every number below
names the one-minute load average it was taken at. The `copy` backend served
ext4; the btrfs volume is a udisks loop image made the S1 way, where the
bench fixture gets the `reflink-walk` backend.

## 1. Result

| Cell | Filesystem | Backend | spare | p50 ms | p95 ms | timing_valid | Line | Met |
|---|---|---|---|---|---|---|---|---|
| `m1-add-100k` | ext4 | `copy` | true | **626** | 2107 | true | p50 <= 1000 | yes |
| `m1-add-100k` | btrfs loop volume | `reflink-walk` | true | **569** | 1084 | true | p50 <= 200 | no |

The ext4 condition is met with the bench's own sample counts (10 warm runs,
seed 20260905, manifest unchanged): `p50_ms` 625.5 at load 5.8 to 6.6,
`spare: true`, `timing_valid: true`. The same cell at `main` (commit
`deaec1a`, before this change) measured 1165 ms at p50 in C9 on a loaded
host and 3.62 s per call under the load of this session (§2); the plan step
that C12 added afterwards had put a walk of golden on the spare path.

The btrfs line is not met, as the brief expected: a spare-served `add` costs
the same claim, checkout, and fixup on both filesystems, and the checkout of
100k entries alone costs 300 to 400 ms on git 2.34.1 (§5.7). The volume
number, 569 ms, sits within the noise of the ext4 one. The CI `loop-fs` job
records the runner number after the merge.

**The checkout share is above 60 %, so G4 opens.** In the per-step lines of
the final code (§5.4 and §5.6) the checkout is CHECKOUT_SHARE_PLACEHOLDER of
the `add` on ext4.

The independent evidence in §5: the bench documents, three `/usr/bin/time`
lines TIMED_SUMMARY_PLACEHOLDER, an empty `git status --porcelain` and an
empty file-level `find` diff on a timed klon, no hunk under `src/bench/` or
`bench/`, and `cargo test` green.

## 2. Where the time went before this change

`KLON_DEBUG=1` prints one line per step of the `add` transaction. On the
persistent 100k fixture (100,000 tracked files over 1000 directories, 20,000
ignored files of 100 kB in `build/`, `feature` with 20 edits and 2 new
files), a spare-served `add` at `main` (commit `deaec1a`) took 3.62 s at
load 18:

```
klon: debug: add resolve-and-probe 33.8 ms
klon: debug: add plan 1739.2 ms
klon: debug: add register 21.2 ms
klon: debug: add claim 7.7 ms
klon: debug: add index 1.6 ms
klon: debug: add config 27.8 ms
klon: debug: add checkout 424.2 ms
klon: debug: add clean 361.7 ms
klon: debug: add fixup 387.8 ms
klon: debug: add hooks 14.5 ms
klon: debug: add env 2.0 ms
klon: debug: add hooks-export 6.3 ms
klon: debug: add status-warm 565.2 ms
klon: debug: add unlock 7.8 ms
klon: debug: add spare-start 3.7 ms
klon: debug: add warm-start 0.2 ms
wall 3.62 s
```

Four of those steps walked the whole tree although the spare made the
walk pointless:

| Step | What walked | Why it was there |
|---|---|---|
| `plan` (1739 ms) | the byte estimate and the warm plan of a clone: one `stat` walk over golden's 120k entries plus `git ls-files --others --ignored` in golden | R41 and R36 need them for a clone. A spare is one rename and writes nothing. C12 added this after C9 measured the spare at 1.16 s. |
| `clean` (362 ms) | `git clean -fdq`: a directory walk over the klon | golden may hold untracked files that the clone copied. The spare almost never does. |
| `fixup` (388 ms) | `git ls-files --others --ignored --directory` in the klon, then the walk over `build/` | the pass asks git for the ignored roots. The spare record already names them. |
| `status-warm` (565 ms) | the untracked scan of the first `git status`, all 1001 directories | the untracked cache had to be built from nothing. |

The checkout (424 ms) is git's own work: read the index, `lstat` 100k
entries, unpack the tree, write the index.

## 3. What this change does

### 3.1 The clone is planned only when the clone happens

`add` now asks `spare::looks_usable` before it plans. That check reads
`spare.json` and the three exclusion files; it runs no git command and takes
no lock. When it passes, the estimate, the free-space check (R41), the warm
plan (R36), and the progress line wait. When the claim then fails anyway
(another `add` took the spare in between, or the lock found it torn), `fill`
plans after the registration and a refusal rolls the `add` back through the
existing cleanup path, so the outcome is the same one process later. The
`two_concurrent_adds_use_the_spare_at_most_once` test exercises that path.

### 3.2 The spare carries its untracked cache, relocated at the claim

The lead in the brief, "run `git status` in golden before the index copy",
does not work as stated, and the reason is worth recording. git keeps the
untracked cache in the `UNTR` extension of the index, and that extension
starts with an identity string:

```
Location /home/navaro/.cache/klon/g1/golden.wt/before-1x, system Linux
```

`ident_in_untracked` in `dir.c` compares it with the worktree git runs in
and drops the cache on a mismatch. A klon moved from `before-1` to
`before-1x` went from `opendir:0` to `node-creation:1000 opendir:1001` on
its next status (§5.7). So a cache from golden, or from the spare's own
location, is dead on arrival in the klon.

The builder therefore runs the forced status *inside the spare*, with
golden's common directory as `GIT_DIR`, the spare as `GIT_WORK_TREE`, and
the copied index as `GIT_INDEX_FILE`, after it wrote the three status config
keys and the `/.klon/` exclude line. The claim then rewrites the identity:
`src/untracked.rs` walks the index entries to the extensions, replaces the
location in the `UNTR` identity, fixes the extension size (and the `EOIE`
hash when that extension is present), and writes a correct SHA-1 trailer,
which git verifies on every read. Every entry byte stays as it was; this is
not the entry splice of G4. The relocation costs one read and one write of
the 8 MB index plus one SHA-1 pass, 70 to 100 ms under load on this host
(`index` in the tables below). An index the code cannot read moves as it is,
and the first status rebuilds the cache as before.

`git checkout` keeps the extension (`move_index_extensions`) and marks only
the directories it wrote. The first status in a fresh klon then opens 21
directories instead of 1001: the 20 that `feature` edits and the root, which
gained `.klon/`. The builder that `add` starts afterwards runs one forced
status in the new klon before it clones, so the cache is written back
complete off the critical path. A status right after `add` reads
`node-creation:0 opendir:21`; one after the builder reads `opendir:0`.

### 3.3 `git clean` gets the list instead of the walk

The status that the builder runs in the spare prints the untracked paths of
the spare, and the builder records them in `spare.json` as `untracked`.
`add` passes exactly those paths to `git --literal-pathspecs clean -fdq --`
and runs no clean at all when the list is empty. A direct clone, or a spare
from an older builder, reads the list from its own status. The semantics do
not change: the paths are the ones `git status` would report, and `clean -d`
removes a fully untracked directory from its collapsed name. A list above
1000 entries falls back to the plain walk.

### 3.4 The fixup runs beside the checkout, from the recorded roots

The spare record lists the ignored directories it holds (`top_mtimes_after`,
the output of the same `git ls-files --others --ignored --directory` that the
fixup pass used to run in the klon). `fixup::run_named` starts from that
list, and since the pass touches only ignored directories and the checkout
never does, the two run on two threads and the slower one sets the pace. A
direct clone keeps the old order: the fixup asks git after the checkout and
runs beside the status.

### 3.5 No status on the critical path of a spare-served `add`

With the relocated cache and the recorded list, the inline status had one
job left: writing 21 re-read directories back. That moved to the builder
(§3.2). The direct-clone path keeps its inline forced status, which is the
G2 change (PR #77) and stays as that PR wrote it.

### 3.6 One git call fewer in the claim

The claim ran `git rev-parse HEAD` to print a debug line about a stale
spare. It runs now only under `KLON_DEBUG=1`.

## 4. The levers that were measured and left alone

| Lever | Finding |
|---|---|
| `index.skipHash` | git 2.40 and later. The host has 2.34.1; the CI floor is 2.34.1. Not available. |
| `index.threads` | Setting it to `true` and forcing an index write on git 2.34.1 produced no `IEOT` extension, so no threaded read. G2 saw the same. Not taken. |
| `checkout.workers` | Parallel checkout helps a checkout that writes many files. This one writes 22. Not taken. |
| `core.preloadIndex` | Already on by default; the trace shows `index:preload` at 57 ms over threads. |
| the journal write count | Five state writes and one delete, each a small file plus a rename with no `fsync`. They do not appear in the per-step lines above 1 ms. Not changed. |
| fewer `git config` calls | `ensure_config` reads once and writes nothing after the first `add` (C9). The remaining `git config --get` of the hooks copy and the worktree-config check cost 2 to 3 ms each on a quiet host. Not changed. |

## 5. Evidence

Every block below is the output of the command it names, unchanged. The
`gh klon` in these commands runs the binary of this branch through a private
extension directory (`XDG_DATA_HOME=$HOME/.cache/klon/g1-xdg`, where
`gh/extensions/gh-klon/gh-klon` is a symlink to `target/release/gh-klon` of
the worktree), because the extension installed for the user points at the
main checkout. `GIT_CONFIG_GLOBAL=/dev/null` and the fixture identity are set
the way the bench sets them for its own commands.

### 5.1 The bench cell on ext4

EXT4_HEAD_PLACEHOLDER

EXT4_BENCH_PLACEHOLDER

### 5.2 Three timed `add` calls with a spare present (ext4)

Each call ran after the builder that the previous one started had finished,
so a spare was present every time. `bench-<i>` is a new branch from `main`,
as the goal names it; the bench cell above checks out `feature`, which
differs from `main` in 22 paths.

`/usr/bin/time` around `gh klon` counts the start-up of the `gh` wrapper,
which the bench does not: the bench runs the klon binary directly. On this
host that start-up alone is a third of a second, measured here first:

EXT4_GH_PLACEHOLDER

The three timed calls, on the same fixture, at the load named before each:

EXT4_TIMES_PLACEHOLDER

A first set of three, taken right after the bench cell finished and while
the load was still between 6.5 and 7, for the record: two of the three sat
above 1.2 s, and the per-step lines of one such call put the difference in
the `register` step (`git worktree add`, 108 ms instead of 20) and the
checkout, both git processes that share the host with everything else.

EXT4_TIMES_FIRST_PLACEHOLDER

### 5.3 The checks on the last timed klon (ext4)

The `find` line for the directory itself (the first line, an empty relative
path) reports the size of the `build` directory inode: ext4 gives a directory
with 20,000 entries a different number of index blocks depending on the order
the entries went in, and the copy backend does not insert them in golden's
order. No file below it differs, which the second `diff` with `-mindepth 1`
shows; the bench's own correctness check (`ignored_manifest: match`) compares
the files, not the directory inode.

EXT4_CHECKS_PLACEHOLDER

### 5.4 The per-step lines after the change (ext4)

EXT4_STEPS_PLACEHOLDER

### 5.5 The bench cell on the btrfs loop volume

The volume is a 20 GB sparse image, `mkfs.btrfs --rootdir` with a user-owned
`klon/` seed, `udisksctl loop-setup` and `udisksctl mount`, mounted at
`/media/navaro/klon-g1`. The bench fixture is a plain directory on it, so the
probe selects `reflink-walk`; a golden that `init --volume` moved onto a
volume is a subvolume and would take `btrfs-snapshot` for the clone, but the
spare-served `add` measured here is the same claim, checkout, and fixup
either way.

BTRFS_HEAD_PLACEHOLDER

BTRFS_BENCH_PLACEHOLDER

The 200 ms line fails, as the brief expected: the checkout alone costs more
than that on git 2.34.1 (§5.7), and the claim does not remove it.

### 5.6 The per-step lines on the volume

BTRFS_STEPS_PLACEHOLDER

### 5.7 The checkout share, from `GIT_TRACE2_PERF`

One spare-served `add` on ext4 at load 15 to 28, with `GIT_TRACE2_PERF` on
every git child (the binary before the review fixes; the git calls are the
same). The sum of the git processes inside `add` and the regions of the
checkout:

```
git processes inside add (wall ms):
  rev-parse         3.4
  worktree          3.0
  show-ref          3.0
  worktree          0.0
  update-ref       16.9
  rev-parse         2.2
  config            2.4
  checkout        382.7
  config            2.3
  rev-parse         2.2
  config            1.9
  status          305.1
  worktree          3.0
top regions of checkout (382.7 ms):
     166.1 ms  index label:do_write_index .../worktrees/after-2/index.lock
     104.0 ms  unpack_trees label:unpack_trees
      68.2 ms  unpack_trees ..label:traverse_trees
      57.5 ms  index label:preload
      31.4 ms  index label:do_read_index .../worktrees/after-2/index
      10.1 ms  cache_tree ..label:update
```

That run still had the inline status (305 ms); the final code runs none on
the spare path. The checkout is read index, `lstat` over the entries,
unpack, and write index, all proportional to the 100k entries and none of it
reachable from outside git on 2.34.1.

CHECKOUT_SHARE_PLACEHOLDER

### 5.8 The lead (1) experiment: the untracked cache is bound to a path

```
1. forced status persists the cache:
0
8076642
node-creation:0 directory-invalidation:0 opendir:0
2. move the klon and repair the worktree link:
repair: gitdir incorrect: /home/navaro/.cache/klon/g1/golden/.git/worktrees/before-1/gitdir
node-creation:1000 directory-invalidation:0 opendir:1001
Location /home/navaro/.cache/klon/g1/golden.wt/before-1x, system Linux
3. move it back:
repair: gitdir incorrect: /home/navaro/.cache/klon/g1/golden/.git/worktrees/before-1/gitdir
node-creation:1000 directory-invalidation:0 opendir:1001
```

The counters come from `GIT_TRACE2_PERF` on `git status --porcelain`; the
`Location` line is the identity string inside the index file.

### 5.9 The relocated cache in a fresh klon

The first status in a klon right after `add`, then after the builder's
background warm (ext4, the final binary):

```
$ git -C /home/navaro/.cache/klon/g1/golden.wt/after-4 status --porcelain
(counters of that status:) node-creation:0 directory-invalidation:0 opendir:21
--- after the builder warmed the klon:
(counters of that status:) node-creation:0 directory-invalidation:0 opendir:0
$ git -C /home/navaro/.cache/klon/g1/golden.wt/after-3 fsck --no-dangling --no-progress
(exit 0)
```

### 5.10 `git diff --stat origin/main -- src/bench/ bench/`

```
$ git diff --stat origin/main -- src/bench/ bench/
(no output)
```

No hunk under `src/bench/` or `bench/`; the manifest seed and the run counts
are unchanged.

### 5.11 The last 20 lines of `cargo test`

CARGO_TEST_PLACEHOLDER

### 5.12 Review

Codex (`gpt-6-astra`, effort high) reviewed the first push and found four
regressions, all real, all fixed with a test each; the PR body has the
table. The evidence above was taken with the fixed binary.

## 6. Acceptance

| AC | State |
|---|---|
| The bench output shows both cells with `timing_valid: true` under the limits | ext4: pass, 626 ms at p50 with `spare: true` (§5.1). btrfs: `timing_valid: true`, 569 ms at p50, above the 200 ms line (§5.5); the report says so and why. |
| The three `/usr/bin/time` lines are each under 1.2 s on ext4 with a spare | TIMED_AC_PLACEHOLDER |
| The `status` output is empty and the `find` diff is empty | pass for the status and for every file below `build/` (§5.3). The spec's exact `find` prints one line for the `build` directory inode itself, whose ext4 size differs by insertion order; the report shows both diffs. |
| The last 20 lines of `cargo test` show `test result: ok` and no failure | pass (§5.11) |
| `bench/manifests/v1.toml` is unchanged; every hunk under `src/bench/` has a justification | pass: no hunk under either path (§5.10) |
